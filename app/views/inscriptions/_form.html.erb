  <div class="form-group">
    <%= f.label :student_id, "Estudiante" %>
    <%= f.collection_select(:student_id, Student.all, :id, :name, {hide_label: true}, class: "form-control m-input m-input--air") %>
  </div>

  <div class="form-group">
    <%= f.label :parallel_id, "Gestion a la cual Corresponde el Curso" %>
    <%= f.collection_select(:parallel_id, Parallel.all, :id, :name, {hide_label: true}, class: "form-control m-input m-input--air") %>
  </div>
