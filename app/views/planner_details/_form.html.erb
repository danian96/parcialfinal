<%= form_for(planner_detail) do |f| %>
  <% if planner_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(planner_detail.errors.count, "error") %> prohibited this planner_detail from being saved:</h2>

      <ul>
      <% planner_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :init_date %>
    <%= f.date_select :init_date %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :response %>
    <%= f.text_area :response %>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.text_field :status %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
